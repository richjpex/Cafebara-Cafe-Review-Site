<nav class="navbar navbar-expand-lg fixed-top" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img src = "../../images/assets/2logo.png" id = "logo">
      <span id = "headername">cafebara</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        {{#unless owner}}
            <li class="nav-item {{#if isIndex}}active{{else}}inactive{{/if}}">
              <a class="nav-link {{#if isIndex}}active{{else}}inactive{{/if}}" aria-current="page" href="/">Home</a>
            </li>
            <li class="nav-item {{#if isCafe}}active{{else}}inactive{{/if}}">
              <a class="nav-link {{#if isCafe}}active{{else}}inactive{{/if}}" href="/cafe">Review</a>
            </li>
            <li class="nav-item {{#if isAbout}}active{{else}}inactive{{/if}}">
              <a class="nav-link {{#if isAbout}}active{{else}}inactive{{/if}}" href="/about">About</a>
            </li>
        {{/unless}}
        {{> usernav}}
      </ul>


    </div>
  </div>
</nav>

